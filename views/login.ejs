<!DOCTYPE html>

<html lang="en" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  

    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    
    <link rel="stylesheet" href="/stylesheets/signup.css" />
  </head>
  <body>
   
    <div class="wrapper">
  
      <h2>Login</h2>
      <form action="/login" method="post">
        <% if(dataLogin && dataLogin.some(error => error.param ===
        'is_verified')) { %>
        <div class="danger">
          <%= dataLogin.find(error => error.param === 'is_verified').msg %>
        </div>
        <% } %>
        <div class="input-box">
          <input
            type="text"
            class="form-control p_input"
            name="email"
            placeholder="Email"
            
          />

          <% if(dataLogin && dataLogin.some(error => error.param === 'email' ))
          { %>
          <div class="danger">
            <%= dataLogin.find(error => error.param === 'email').msg %>
          </div>
          <% } %>
        </div>

        <div class="input-box">
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Enter password"
            
          />
          <% if(dataLogin && dataLogin.some(error => error.param ===
          'password')) { %>
          <div class="danger">
            <%= dataLogin.find(error => error.param === 'password').msg %>
          </div>
          <% } %>
        </div>
    
        <div class="policy">
          <a class="password" href="/password">Forgot Password</a>
        </div>
      
        <div class="input-box button">
          <input type="Submit" value="Login" />
        </div>
        <div class="text">
          <h3>Don't have an account? <a href="/">SignUp now</a></h3>
        </div>
      </form>
    </div>
  </body>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <script src="/javascripts/script.js"></script>
</html>
