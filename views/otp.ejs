<!DOCTYPE html>

<html lang="en" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  

    <link rel="stylesheet" href="/stylesheets/signup.css" />
  </head>
  <body>
    <div class="wrapper" style="height: 400px;">
      <h2>Enter OTP</h2>
      <form action="/password/postotp" method="post">
       
        <% if(dataLogin && dataLogin.some(error => error.param ===
        'code')) { %>
        <div class="danger">
          <%= dataLogin.find(error => error.param === 'code').msg %>
        </div>
        <% } %>
       

        <div class="input-box">
          <input
            type="number"
            class="form-control p_input"
            name="otp"
            placeholder="Enter OTP"
            
          />

          <% if(dataLogin && dataLogin.some(error => error.param === 'otp' ))
          { %>
          <div class="danger">
            <%= dataLogin.find(error => error.param === 'otp').msg %>
          </div>
          <% } %>
        </div>

        
        <div class="input-box button">
          <input type="Submit" value="Verify OTP" />
        </div>
        <div class="text">
          <h3>Don't have an account? <a href="/">SignUp now</a></h3>
        </div>


      </form>
    </div>
  </body>
</html>
